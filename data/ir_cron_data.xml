<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Bekleyen İşlemleri Kontrol Et -->
        <record id="ir_cron_check_pending_transactions_turkey_pos" model="ir.cron">
            <field name="name">POS: Bekleyen İşlemleri Kontrol Et</field>
            <field name="model_id" search="[('model', '=', 'payment.transaction')]" model="ir.model"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[model._cron_check_pending_transactions()]]></field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Günlük Mutabakat Raporu -->
        <record id="ir_cron_daily_reconciliation_turkey_pos" model="ir.cron">
            <field name="name">POS: Günlük Mutabakat Raporu</field>
            <field name="model_id" search="[('model', '=', 'pos.reconciliation')]" model="ir.model"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[model._cron_daily_reconciliation()]]></field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Eski İşlemleri Arşivle -->
        <record id="ir_cron_archive_old_transactions_turkey_pos" model="ir.cron">
            <field name="name">POS: Eski İşlemleri Arşivle</field>
            <field name="model_id" search="[('model', '=', 'payment.transaction')]" model="ir.model"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[model._cron_archive_old_transactions()]]></field>
            <field name="interval_number">1</field>
            <field name="interval_type">weeks</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Başarısız İşlemleri Temizle -->
        <record id="ir_cron_cleanup_failed_transactions_turkey_pos" model="ir.cron">
            <field name="name">POS: Başarısız İşlemleri Temizle</field>
            <field name="model_id" search="[('model', '=', 'payment.transaction')]" model="ir.model"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[model._cron_cleanup_failed_transactions()]]></field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>
